// Importing the 'createContext' function from the 'react' module
import { createContext } from "react";

// Creating a new context using the createContext function
const Context = createContext();

// Exporting the created context as the default export
export default Context;
